# Setup

1. Install Python 3.10+.
2. Enter `backend`.
3. Create a virtual environment.
4. Install requirements.
5. Run `python app.py`.
6. Open `http://127.0.0.1:5000`.
7. Register and log in.

For production, use a strong secret through `LIFELINK_SECRET_KEY` and a production WSGI server.
